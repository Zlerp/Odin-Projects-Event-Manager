# Odin-Projects-Event-Manager

#### Ruby Project 

The Event Manager parses a CSV file with bad data and repairs it.

######Skills Learned 
- Parsing data
- Parsing CSV data
- `slicing` strings
- `rjusting` strings 
